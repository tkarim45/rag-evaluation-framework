.env
__pycache__/
*.pyc
.venv/
venv/
.deepeval/
.DS_Store
*.log

# Vector store + caches are rebuildable — never commit.
chroma_db/
.cache/

# Deliverables ARE committed: testset/ (frozen benchmark), results/*.json (immutable runs),
# reports/ (writeups + charts), corpus/ (source docs). Only ignore scratch result files.
results/smoke*.json
testset/smoke*.json
